# Assuming that you have access to the itet-stor/.../covlus_bmicnas02/ drive you can simply set you username in this file.
# When running the code and the notebooks on the cluster itself this should be sufficient to adjust all paths automatically.
CLUSTER_USERNAME = 'mrichte'